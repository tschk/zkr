[package]
name = "zkr"
version = "0.4.2"
edition = "2024"
rust-version = "1.85"
description = "Evidence-backed temporal memory for personal agents"
repository = "https://github.com/tschk/zkr"
license = "ISC"
exclude = ["plugins/"]

[dependencies]
rusqlite = { version = "0.37", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "2"

[dev-dependencies]
criterion = "0.8.2"
tempfile = "3"

[[bench]]
name = "retrieval_stats"
harness = false

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
